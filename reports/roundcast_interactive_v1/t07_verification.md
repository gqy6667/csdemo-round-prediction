# T07 — 比赛分析视图与共享导航验收

日期：2026-09-05。结论：T07 通过。本轮仅在本地保存，未提交或推送。此前 GitHub 最新提交为 `db6242a`；下一项 T08 为正式指标 API，技术视图仍未开放。

## 交付与操作

[打开比赛分析示例](http://127.0.0.1:8765/#view=analyst&case=B&stage=post_first_kill&algorithm=lightgbm)。服务需在本机运行；原页面先刷新，然后点击顶部“比赛分析”，再点“运行全部对比”。

- 离散点图：购买结束、首杀后各两项真实 CT 胜率。圆点为 XGBoost，方点为 LightGBM，外圈标记当前组合；固定 0–100% 轴，不连接成连续预测曲线。
- 同算法两时点变化：`(首杀后概率 − 购买结束概率) × 100`，单位为百分点；两项均成功才显示。不是当前四项表下方的“同一时点两算法差”，也不是首杀贡献或因果效果。
- 经济对比：装备价值、剩余现金及各自 `CT − T` 差额；均为购买结束状态，不冒称首杀后的库存或收入。
- 只读输入：固定白名单 27/31 个字段，中文名称、数值、单位/编码、字段名。缺失显示“未提供”，不当作 0；未知字段不展示。
- 共用案例、时点、算法、四项结果、赛果揭示和既有 Codex 对话。仅切视图不读取新快照、不运行模型；相同案例已有结果保持，换案例清空。
- 地址只保存四个选项。刷新仅恢复这些选项，不恢复预测、赛果、聊天或请求标识，也不自动运行。

图表只描述当前筛选回合；整个演示仅有三个案例，不可用来估计总体准确率或算法排名。输入表可纵向滚动，窄屏点图与宽表可在自身区域横向滚动，不撑宽整个页面。

## 真实数据与浏览器核对

[机器可读证据](t07-browser-evidence.json) 中 `matrix` 是三个案例 × 四组合的 12 个实际图点及其正式参考。每个值来自浏览器发起的真实模型推理，逐项绝对误差不超过 `1e-8`；没有用参考表给页面补概率。

核对通过：

1. 带 B / 首杀后 / LightGBM 的链接直接打开分析页；不自动预测、不读取隐藏赛果。
2. 12 个图点逐项与各自正式参考、四项表一致，原始概率只在显示时舍入。
3. 六个案例/时点快照逐字段与实际接口相同，购买结束为 27 项、首杀后为 31 项。前端白名单另与后端两组正式输入字段进行集合核对。
4. 两项经济差、同算法前后变化与原始值一致。仅切视图、前进/后退无新增 API 调用，同案例请求标识保持不变。
5. 显式揭示后两个视图显示相同赛果；刷新保留选择，但预测、赛果和聊天不恢复。
6. 受控返回一个 HTTP 503 后，该点及关联前后变化为空，其余三个点保留。单项重试只增加一次真实请求，恢复缺失点。
7. 真实预测被暂时延迟时切换视图，原四项批次继续完成，没有重复调用。无效地址规范化到支持的默认选择，不请求未知接口。
8. 1440、1280、390 像素宽度页面无横向溢出；真实截图见下方。浏览器页面异常列表为空。

[1440 桌面](t07-analyst-1440.png) · [1280 桌面](t07-analyst-1280.png) · [390 窄屏](t07-analyst-390.png)

截图为 B / Mirage R11 的实际四项结果，赛果未揭示；没有玩家、位置、烟雾或炸弹占位数据。本轮未发送新的 Codex 问题，不把本地登录状态当成远端回复证明；原有真实对话验收见 [对话记录](chat_verification.md)。

## 增量与独立审查

- 新增五项 JavaScript 行为测试，先观察路由解析、共享导航、分析派生数据尚不存在的失败，再分别实现导航与数据层并通过。
- 新增页面合同先因缺少分析区域失败，再接入灰白页面、点图、经济对比与只读表。
- 复用原有测试和单一控制器，不复制模型调用、比较结果或聊天状态；模型代码及工件未改动。
- 只读审查核对了 27/31 字段单位与边界。有护甲人数不是护甲值；步枪总类与 AK/M4 子类不可相加；投掷物库存不是烟雾数或投掷次数。
- 审查提出过“异步结果可能覆盖待处理 hash 导航”的怀疑。定向真实浏览器测试及事件记录显示 `popstate` 在 hash 赋值返回前已更新视图，预测完成没有覆盖跳转。该阻断结论已撤销；没有为未证实的问题改产品逻辑。保留定向测试和 `navigationRaceTrace` 作为回归证据。

## 最终验证

| 检查 | 结果 |
| --- | --- |
| JavaScript 状态测试 | 共 31 项：原状态 6、选择 7、对话 13、分析 5；由 Python 合同测试执行 |
| Python 仓库全套 | 346 项通过，22.703 秒；包含执行 JavaScript 的合同检查，计数不简单相加 |
| 实际 Chrome | 12 图点、六快照、导航/刷新、受控失败重试、运行中切页、定向导航竞态检查全部通过 |
| 编译与 Git 空白检查 | 通过；仅有现有 Windows 换行转换提示 |

```powershell
& 'C:\Users\admin\11\envs\game\python.exe' -m unittest discover -s tests -p 'test_*.py'
& 'C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' scripts/verify_roundcast_analysis_browser.cjs
& 'C:\Users\admin\11\envs\game\python.exe' -m compileall -q src
git diff --check
```

浏览器脚本复用 `http://127.0.0.1:8765/` 上的既有服务，不关闭该预览；可用 `ROUNDCAST_URL` 指向明确的其他本机测试服务。换机时修改 Python/Node/Chrome 环境路径并另行迁移 Git 忽略的工件。

## 下一阶段

T08：固定来源读取四模型正式评估指标并提供本地 API，区分购买结束 4,172 和首杀后 4,170 个测试回合。T09 再把这些指标与版本、哈希、输入验证接入精简技术页面。本轮未进入这些阶段，也未重训、加预测时点、公开部署、录视频或同步 Figma。
