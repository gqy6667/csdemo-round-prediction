# T06 — 三案例选择与四模型对比验收

日期：2026-09-05。结论：T06 通过。下一项为 T07 比赛分析视图；不代表三页成品已经完成。

## 已交付

- 三个真实案例：A / Ancient R4、B / Mirage R11、C / Nuke R17。
- 购买结束、首杀后两个可点击时间节点；每个时点可选择 XGBoost 或 LightGBM。
- “运行当前组合”和“运行全部对比”。全部对比对当前案例逐项发出四次真实预测请求，各行独立显示未运行、运行中、成功或失败；失败项可单独重试。
- 首杀后显示有来源的首杀阵营、时间、武器与爆头事实。购买结束页不提前显示这些字段。输入字段数分别显示 27 / 31。
- 主胜率始终对应当前选择；不是最后完成的任意一行。案例切换清除旧概率、赛果揭示和旧批次，迟到结果不能覆盖新案例。
- 对话只附带当前选择的快照及匹配的成功预测。切换案例或时点重置追问上下文，旧回答仍保留原标签；同一案例/时点切算法可继续比较。

## 真实浏览器证据

[机器可读记录](t06-browser-evidence.json) 保存实际 HTTP 预测响应、模型哈希、请求标识、参考核对与检查结果。`predictions` 前 12 项为完整矩阵，后续条目为故障与重试阶段附加的真实响应，不代表新增演示组合。正常矩阵未使用模拟概率。

| 案例 | 购买结束 XGB | 购买结束 LGBM | 首杀后 XGB | 首杀后 LGBM |
| --- | ---: | ---: | ---: | ---: |
| A / Ancient R4 | 62.9153% | 65.9115% | 76.5654% | 73.6278% |
| B / Mirage R11 | 73.3658% | 77.3513% | 41.5059% | 39.9134% |
| C / Nuke R17 | 97.4894% | 97.0418% | 98.5496% | 98.9839% |

以上均为 CT 回合胜率，仅为展示而四舍五入。12 项真实值均通过与各自冻结参考的绝对误差不超过 `1e-8` 检查；12 个请求标识互不相同。界面表格和主胜率均与对应响应一致，切换选项本身不发起预测或读取隐藏赛果。

故障场景单独进行受控拦截，不用于正常矩阵：

1. 一项返回 HTTP 503，其余三项保留真实成功结果；单项重试仅再发出一次预测。
2. 延迟真实预测期间重复点击全部对比，不生成重复批次；切换案例后旧批次停止后续请求，迟到的旧结果不能覆盖当前状态。
3. 主动揭示后切换案例，赛果恢复隐藏。购买结束隐藏首杀面板；首杀后显示对应案例事实。
4. 1440、1280、390 像素宽度均无页面横向溢出，宽表在自身容器内滚动；浏览器页面异常列表为空。

截图：[桌面 1440](t06-viewer-1440.png)、[桌面 1280](t06-viewer-1280.png)、[窄屏 390](t06-viewer-390.png)、[受控单项失败](t06-controlled-partial-failure.png)。截图中揭示的实际赢家属于显式操作后的状态，不是默认剧透。

隔离浏览器验收服务运行于受限测试环境，对话登录检查可能显示不可用；本机正常环境下的预览服务另行检查，首页返回 200，`/api/chat/status` 返回可用且已登录。本轮未再发送真实 Codex 问题，不把登录状态当作在线回复证明；此前真实网页回复证据见 [对话验收](chat_verification.md)。

## 测试先行与最终结果

- 先新增六快照 HTTP 测试，观察新路径返回 404，再实现固定案例/时点路径；快照无赛果和参考概率。
- 新选择与全部对比测试先因未提供行为失败，再实现选择状态、独立对比行、单项重试和批次代次隔离。
- 独立审查发现首杀展示字段缺失/类型不符可能崩溃或误显示。新增失败测试后，前端现验证首杀阵营、时间、爆头、武器及渲染所用基础字段；不合法快照不会发布为就绪状态。
- 新增对话跨案例/时点及迟到回复隔离测试，保留此前全部对话测试。

最终代码版本通过：

| 检查 | 结果 |
| --- | --- |
| JavaScript 状态测试 | 26 项通过：选择 7、原状态 6、对话 13 |
| Python 仓库全套 | 344 项通过，22.139 秒；包含调用 JavaScript 测试的合同检查，不将两者当成完全独立计数 |
| 实际 Chrome 浏览器 | 12 组合参考核对、部分失败/重试、快速切换、三个宽度均通过 |
| Python 编译、Git 空白检查 | 均通过；Git 仅提示现有 Windows 换行转换 |

复验命令（沿用现有环境，不新增依赖）：

```powershell
& "C:\Users\admin\11\envs\game\python.exe" -m unittest discover -s tests -p "test_*.py"
& "C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" --test tests/roundcast_state.test.cjs tests/roundcast_selection.test.cjs tests/roundcast_chat.test.cjs
& "C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe" scripts/verify_roundcast_selection_browser.cjs
& "C:\Users\admin\11\envs\game\python.exe" -m compileall -q src
git diff --check
```

浏览器脚本使用本机已有 Chrome/Playwright 路径，换机需调整运行环境路径；模型工件和 Git 忽略的数据仍需另外迁移。

## 保持的边界与下一步

模型、校准器、输入数据、冻结来源及参考概率未改动。首杀后经济字段仍是购买结束状态；两算法概率差以百分点显示，不当作显著优劣或因果效果。没有插值连续胜率、自动读取最终赢家、开放输入修改、重训、公开部署或录制视频。

本轮先按授权上传已有 T01–T05 与对话内容（`edfc541`），再独立提交 T06。下一项 T07 增加比赛分析视图、两时点概率对比图、装备差及只读快照，复用同一选择与结果。
